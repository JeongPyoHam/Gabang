<UserControl x:Class="Gabang.Controls.VariableGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gabang.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Value" Binding="{Binding VariableValue}" />
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" />
            </DataGrid.Columns>
            <DataGrid.RowHeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding VariableName}" />
                </DataTemplate>
            </DataGrid.RowHeaderTemplate>
        </DataGrid>
</UserControl>
